/** ################################## TERNARIOS Y NULL CHECK ##################################
 *

*/


let firstName; 
let lastName = 'Ugaz';

// console.log(`${firstName || 'No fisrtName'} ${lastName || 'No lastName'}`)

const isActive = true

const message = ( isActive ) 
                    ? 'Activo' 
                    : 'Inactivo'

console.log(message)
 
